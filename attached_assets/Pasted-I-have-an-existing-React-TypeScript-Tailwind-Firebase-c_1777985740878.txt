I have an existing React + TypeScript + Tailwind + Firebase chat app 
called SweeTalk. I want to add a complete games system to it.
DO NOT touch any existing chat logic. Only add new files and make 
minimal surgical changes to Chat.tsx for layout integration.

## ─────────────────────────────────────
## 🗂️ EXISTING LAYOUT (DO NOT BREAK)
## ─────────────────────────────────────
The current Chat.tsx layout is:
  <div h-screen flex flex-col>           ← root
    <header />                           ← top bar
    <div flex flex-1 overflow-hidden>    ← body
      <div flex flex-col flex-1>         ← chat column (main)
        <SharedNote />
        <messages scroll area />
        <input bar />
      </div>
      {sidebarOpen && (
        <aside w-72 border-l>           ← RIGHT sidebar
          <Sidebar />                   ← has Anniversary, Prompts, Album
        </aside>
      )}
    </div>
  </div>

The Sidebar component currently lives inside Chat.tsx.
It has: Anniversary counter, Sweet prompt, Photo Album button.

## ─────────────────────────────────────
## 🎯 NEW LAYOUT BEHAVIOR
## ─────────────────────────────────────

Add a "🎮 Play a Game" button at the bottom of the Sidebar component.

When clicked:
1. Close the right sidebar (setSidebarOpen(false))
2. Open the game panel on the LEFT side of the chat
3. The new layout becomes:
   <div flex flex-1 overflow-hidden>
     <aside w-96 border-r>   ← GAME PANEL (left, new)
       <GamePanel />
     </aside>
     <div flex flex-col flex-1>  ← chat column (right, unchanged)
       ... all existing chat ...
     </div>
   </div>

4. A "Close Games" (✕) button in the GamePanel header closes it
   and optionally reopens the right sidebar.
5. On mobile (< md): game panel takes full screen with a 
   "Back to Chat" button fixed at top.
6. Both panels are independently scrollable.
7. Game results and events can be sent to chat as special 
   "game" type messages (a game bubble in the message list).

## ─────────────────────────────────────
## 🤖 PANDA AI INTEGRATION
## ─────────────────────────────────────
All games use the Anthropic Claude API (called "Panda" in the UI)
for question generation, evaluation, memory, and personalization.

Create lib/panda.ts:

  const PANDA_SYSTEM = `
    You are Panda, a warm, playful, and emotionally intelligent AI 
    assistant built into SweeTalk — a private chat app for two people 
    in a relationship. Your job is to make their games more personal, 
    fun, and meaningful. You remember context from previous games 
    within this session. You speak with warmth and light humor. 
    Never be generic. Always try to reference what you know about 
    their relationship when relevant.
  `;

  interface PandaMemory {
    gameHistory: { game: string; result: string; date: string }[];
    knownFacts: string[];      // things Panda has learned about them
    streaks: { [game: string]: number };
    totalPoints: { player1: number; player2: number };
  }

  // Store memory in Firestore: pandaMemory/{sharedRoomId}
  // Load it at app start, update after each game round

  export async function askPanda(
    userMessage: string,
    memory: PandaMemory,
    extraContext?: string
  ): Promise<string>
  // Calls Anthropic API with system prompt + memory context injected
  // Returns Panda's response text

  export async function generateQuestion(
    gameType: string,
    memory: PandaMemory,
    category?: string
  ): Promise<{ question: string; hint?: string; options?: string[] }>
  // Asks Panda to generate a personalized question for the given game
  // Panda uses memory to avoid repeats and make questions personal

  export async function evaluateAnswer(
    question: string,
    correctAnswer: string,
    guessedAnswer: string
  ): Promise<{ correct: boolean; score: number; pandaComment: string }>
  // Panda evaluates closeness of answer (not just exact match)
  // Returns a score 0–100 and a warm comment

  export async function generateReveal(
    question: string,
    answer1: string,
    answer2: string
  ): Promise<string>
  // Panda writes a warm/funny comment after both answers are revealed

  export async function updateMemory(
    current: PandaMemory,
    gameEvent: object
  ): Promise<PandaMemory>
  // Panda reads the event and updates knownFacts, history, streaks

## ─────────────────────────────────────
## 🎮 GAME PANEL SHELL
## ─────────────────────────────────────
Create components/games/GamePanel.tsx:

  State:
  - activeGame: string | null   (which game is open)
  - pandaMemory: PandaMemory    (loaded from Firestore on mount)

  Layout:
  ┌─────────────────────────────┐
  │ 🎮 Games      [✕ Close]    │  ← header
  ├─────────────────────────────┤
  │ 📊 Scoreboard mini strip    │  ← points + streak
  ├─────────────────────────────┤
  │  [activeGame UI]            │  ← game content
  │  OR                         │
  │  [game menu grid]           │  ← when no game active
  └─────────────────────────────┘

  Game menu grid (when no game active):
  - 3x4 card grid of all games
  - Each card: emoji + game name + short description + "Play" button
  - Highlight "One Question a Day" if not yet answered today
  - Show streak badge on Truth or Dare, Rapid Fire cards

  When a game is active:
  - Show a back arrow "← Games" to return to menu
  - Render the specific game component

  "Send to chat" button appears after each game result:
  - Sends a special game message to the Firestore messages collection
  - type: "game", gameType: string, result: string, emoji: string
  - Renders as a special game bubble in MessageBubble.tsx
    (rounded card with 🎮 icon, game name, result summary, 
     animated confetti if it was a win/match)

## ─────────────────────────────────────
## 🎲 GAME 1: THIS OR THAT
## ─────────────────────────────────────
Create components/games/ThisOrThat.tsx

Flow:
1. Panda generates a This or That question with two options
   e.g. "Call or Text?" / "Morning cuddle or Goodnight message?"
2. Player 1 picks secretly (stored in Firestore, hidden)
3. Player 2 picks secretly
4. When both have answered → REVEAL screen
5. If match: 🎉 "You both chose X!" + confetti animation
   If different: show both choices side by side, 
   Panda writes a funny/sweet comment about the difference
6. "Next Round" → Panda generates a new question
7. "Send to chat" → post result as game bubble

Firestore: games/thisorthat/rounds/{roundId}
  { question, option1, option2, answer1, answer2, 
    status: "waiting"|"revealed", createdAt }

State machine in component:
  "loading" → "waiting_p1" → "waiting_p2" 
  → "both_answered" → "revealed" → "next"

Show animated loading state with Panda avatar while 
Panda is generating the question.

## ─────────────────────────────────────
## 💬 GAME 2: ANSWER & REVEAL
## ─────────────────────────────────────
Create components/games/AnswerReveal.tsx

Flow:
1. Panda generates a personal open-ended question
   e.g. "What is one thing you never say out loud?"
2. Each person types their answer in a private text field
3. "Lock in answer" button → store encrypted answer in Firestore
4. Once both locked → REVEAL
5. Show both answers side by side with names
6. Panda writes a warm, thoughtful comment comparing them
7. Option to "Send to chat"
8. Panda updates memory with interesting facts learned

Firestore: games/answerreveal/rounds/{roundId}
  { question, answer1, answer1Locked, answer2, answer2Locked,
    pandaComment, status, createdAt }

UI detail:
- While waiting for partner: show "Waiting for [name] to answer..."
  with animated dots and Panda peeking animation
- Answers hidden (•••) until both are locked
- Reveal uses a flip card animation (CSS 3D transform)

## ─────────────────────────────────────
## 🔍 GAME 3: GUESS MY ANSWER
## ─────────────────────────────────────
Create components/games/GuessMyAnswer.tsx

Flow:
1. Panda generates a personal question
2. Player A (answerer) types their real answer secretly
3. "Lock answer" → hidden in Firestore
4. Player B (guesser) sees the question and types their guess
5. "Submit guess" → Panda evaluates:
   - Is the guess correct or close?
   - Score: 100 (exact), 75 (very close), 50 (somewhat), 0 (wrong)
   - Panda writes a comment: "So close! She said X, you guessed Y"
6. REVEAL: show real answer + guess + Panda's evaluation
7. Add score to running total in PandaMemory
8. Players alternate who is the answerer each round

Roles:
- Determine answerer vs guesser based on whose turn it is
  (stored in Firestore: currentAnswerer uid)
- After each round, swap roles

Firestore: games/guessmyanswer/rounds/{roundId}
  { question, realAnswer, guess, score, 
    pandaComment, answererId, guesserId, status }

## ─────────────────────────────────────
## 🔓 GAME 4: SECRET UNLOCK
## ─────────────────────────────────────
Create components/games/SecretUnlock.tsx

Flow:
1. One person creates a "locked secret":
   - Write a question (e.g. "When did we first talk?")
   - Write the correct answer
   - Write the hidden message/surprise (unlocked if correct)
   - Optionally attach a secret type: 
     💌 Love note | 📸 Memory | 🎵 Song | 💫 Surprise
2. Partner sees the locked state:
   - Question shown, answer input shown
   - Animated padlock icon
3. Partner submits answer → Panda evaluates (fuzzy match)
4. If correct: padlock opens with animation → reveal the secret
5. If wrong: shake animation, "Try again" + hint from Panda
6. After 3 wrong attempts: Panda gives a bigger hint

Firestore: games/secretunlock/secrets/{secretId}
  { question, answer, secret, secretType, 
    createdBy, attempts, unlocked, unlockedAt }

UI:
- Locked state: dark card with glowing padlock 🔒
- Unlock animation: padlock opens, card flips to reveal
- Confetti on unlock
- List of past secrets: "Locked" vs "Unlocked ✓" history

## ─────────────────────────────────────
## 📅 GAME 5: ONE QUESTION A DAY
## ─────────────────────────────────────
Create components/games/OneQuestionADay.tsx

Flow:
1. Every calendar day, Panda generates one question at midnight
   (or on first visit of the day)
2. Both players answer it (text input)
3. Once both answer, answers are revealed with Panda's comment
4. Streak counter: how many days in a row both answered
5. Calendar view: browse past days (answered vs missed)

Question categories (Panda picks based on mood + history):
- What made you smile today?
- What are you missing right now?
- What is one thing you want tomorrow to feel like?
- What is a small thing that made today better?
- (Panda generates unique ones based on memory)

Firestore: games/dailyquestion/days/{YYYY-MM-DD}
  { question, answer1, answer2, answer1LockedAt, 
    answer2LockedAt, pandaComment, bothAnswered }

UI:
- Today's card: prominent, large text question
- "Both answered!" badge with Panda's comment
- Streak banner: "🔥 12 day streak!"
- Scrollable history of past days below
- Missed days shown in muted color

## ─────────────────────────────────────
## 🧠 GAME 6: MEMORY QUIZ
## ─────────────────────────────────────
Create components/games/MemoryQuiz.tsx

Flow:
1. Panda generates multiple-choice questions about the relationship
   (Panda uses memory + chat history patterns)
   e.g. "What was the first thing you noticed about each other?"
        Options: A) Her smile  B) His voice  C) The way she laughs
2. Player selects answer within 20 seconds (countdown timer)
3. After time or selection → reveal correct answer
4. Score: 3 pts for correct, 1 pt if time ran out
5. 5-question rounds, total score shown at end
6. Panda gives an overall comment: "You know each other well!"

Firestore: games/memoryquiz/sessions/{sessionId}
  { questions: [], answers: [], scores: {}, 
    currentQuestion: number, status, createdAt }

UI:
- Question card slides in from right (CSS transition)
- 4 answer options as big tap-friendly buttons
- Timer ring around the question number (SVG circle animation)
- Score counter top-right
- Results screen: question-by-question breakdown

## ─────────────────────────────────────
## 🎭 GAME 7: SMART TRUTH OR DARE
## ─────────────────────────────────────
Create components/games/TruthOrDare.tsx

Flow:
1. Player picks: Truth 💬 or Dare 💥
2. Pick category: Funny | Cute | Deep | Flirty | Challenge
3. Panda generates a truth question or dare challenge 
   (personalized based on memory and previous rounds)
4. Player completes it (text response for truth / confirmation for dare)
5. Partner reacts with one emoji
6. Streak: how many rounds in a row completed

Panda generates from category + memory context:
  Truth examples:
  - "What is one thing you never say out loud?"
  - "When do you feel most cared for?"
  Dare examples:
  - "Send a voice note saying something nice"
  - "Describe me in three words right now"
  - "Pick a song that reminds you of me and share it"

Firestore: games/truthordare/rounds/{roundId}
  { type: "truth"|"dare", category, prompt, 
    completedBy, completedAt, partnerReaction, pandaComment }

UI:
- "Truth" button: soft blue glow
- "Dare" button: warm orange glow
- Category pills: tap to select
- Dare card flips in dramatically
- Reaction emoji picker for partner
- History scroll below: last 10 rounds

## ─────────────────────────────────────
## ✍️ GAME 8: COMPLETE THE SENTENCE
## ─────────────────────────────────────
Create components/games/CompleteSentence.tsx

Flow:
1. Panda generates a sentence starter:
   "I feel happiest when you..."
   "My favorite thing about you is..."
   "I wish we could..."
2. Both players complete it independently
3. Reveal both completions side by side
4. Panda writes a warm combined response reading both answers together
5. "Save as memory" option: saves the pair to Firestore memories

Sentence styles (Panda picks):
  - Romantic: "The moment I feel most loved is..."
  - Playful: "If you were a snack, you'd be..."
  - Reflective: "One thing I want us to always remember is..."
  - Wishful: "If we had one free day together right now, I'd..."

Firestore: games/completesentence/rounds/{roundId}
  { starter, completion1, completion2, 
    savedAsMemory, pandaComment, createdAt }

UI:
- Sentence starter in large italic font
- Text input below: "...continue the sentence"
- Partner's completion revealed with name tag
- Side-by-side comparison with decorative divider
- Save button: 💾 "Save as memory" → stores to memories collection

## ─────────────────────────────────────
## ⚡ GAME 9: RAPID FIRE
## ─────────────────────────────────────
Create components/games/RapidFire.tsx

Flow:
1. 30-second countdown starts
2. Questions flash one by one (Panda generates 15 questions)
3. Player types fast answers (Enter to submit, skip allowed)
4. After time: show all Q&A pairs
5. Partner can rate each answer: ✓ Accurate | ~ Close | ✗ Off
6. Score = rated answers × multiplier

Question bank (Panda picks fast, fun questions):
  - Favorite food right now?
  - Morning or night person?
  - One word to describe today?
  - What are you craving?
  - Next place you want to visit?
  - Favorite thing to do on a Sunday?

Firestore: games/rapidfire/sessions/{sessionId}
  { questions: [], answers: [], ratings: [], 
    timeStarted, timeEnded, score, status }

UI:
- Big countdown ring (30s, animated SVG circle depleting)
- Question slides up from bottom with each new one
- Text input always focused
- Answered count badge: "7 / 15"
- After time: scroll through all answers as cards
- Rating buttons on each answer card

## ─────────────────────────────────────
## 🎲 GAME 10: RANDOM QUESTION GENERATOR
## ─────────────────────────────────────
Create components/games/RandomQuestion.tsx

Flow:
1. Big "✨ Get a Question" button
2. Panda generates a random question (never repeats from memory)
3. Both can type optional answers
4. "New Question" → next one
5. No scoring, pure conversation starter

Categories toggle at top:
  🌙 Dreamy | 😂 Funny | ❤️ Romantic | 🧠 Deep | 🎉 Fun

Firestore: games/randomquestion/asked (array of past questions)
  → Panda avoids repeats using this list

UI:
- Question displayed in a large beautiful card
- Subtle gradient background per category
- Panda avatar winking in corner
- "Both answer" mode toggle: show text inputs for both

## ─────────────────────────────────────
## 📊 GAME 11: SCOREBOARD
## ─────────────────────────────────────
Create components/games/Scoreboard.tsx

Shows:
- Total points per player (from all games combined)
- Per-game high scores
- Current streaks (daily question, truth or dare, rapid fire)
- Recent game history (last 20 rounds across all games)
- "Most matched" stat: which game you agree on most
- Panda's analysis: "You've played 47 rounds together! 
  Your best game is Answer & Reveal with 89% match rate."

Firestore: pandaMemory/{roomId}
  { totalPoints, streaks, gameHistory, matchRates, knownFacts }

UI:
- Stat cards at top (big numbers)
- Tabs: Overview | History | Stats
- History: timeline of games played (icon + name + result + date)
- Stats: bar chart (recharts) of match % per game

## ─────────────────────────────────────
## 😊 GAME 12 & 13: MOOD CHECK + MOOD SYNC
## ─────────────────────────────────────
Create components/games/MoodSync.tsx

Mood Check flow:
1. Each person picks a mood from animated emoji icons:
   😊 Happy | 😔 Missing you | 😴 Tired | 🤩 Excited 
   😌 Calm | 😤 Frustrated | 🥰 Loving | 😂 Silly
2. Both moods revealed simultaneously
3. If match: "You're in sync! ✨" + special animation
4. If different: Panda writes a gentle prompt 
   e.g. "She's missing you. Maybe send something sweet?"
5. Mood history graph: last 7 days of moods per person

Mood Sync (upgrade):
- After reveal, Panda suggests an activity:
  Both tired → "Maybe share a voice note instead of texting tonight"
  Both excited → "Play Rapid Fire! You're both energized"
  Mismatched → "Try Complete the Sentence to reconnect"

Firestore: games/moods/{YYYY-MM-DD-HH}
  { mood1, mood2, moodedAt1, moodedAt2, pandaSuggestion }

UI:
- Mood grid: 8 large emoji buttons with subtle glow on hover
- Secret selection (blurred until both picked)
- Reveal: two mood bubbles float up and collide 
  → spark animation if match
- Mood history: small emoji timeline for last 7 days

## ─────────────────────────────────────
## 🌍 GAME 14: BUILD OUR WORLD
## ─────────────────────────────────────
Create components/games/BuildOurWorld.tsx

Concept:
A shared digital space that grows over time as you play games.
Each item unlocked is a piece of your world together.

World items (unlocked by milestones):
  🌱 A sprout       → after first game played
  🌸 Cherry blossom → after 7-day streak
  ⭐ A star         → after 10 rounds total
  🏡 A little house → after 30 rounds total
  🌙 A moon         → after 30-day daily question streak
  🌊 Ocean waves    → after 50 rounds total
  🦋 Butterfly      → after 5 perfect Guess My Answer scores
  📚 A bookshelf    → after 20 Complete the Sentence saves
  🎵 Music notes    → after 10 Truth or Dare songs shared
  💫 Shooting star  → after 100 rounds total

UI:
- A beautiful illustrated scene (SVG-based or CSS art)
- Items appear one by one as unlocked (fade-in animation)
- Locked items shown as silhouettes with "?" 
  and "Play X more rounds to unlock"
- Tap any item → shows the milestone that unlocked it + date
- At the bottom: progress bar to next unlock
- Panda narrates: "Your world is growing! 
  3 more rounds until your star appears."

Firestore: games/ourworld/{roomId}
  { unlockedItems: string[], milestones: {} }

## ─────────────────────────────────────
## 🗃️ FIRESTORE STRUCTURE (NEW COLLECTIONS)
## ─────────────────────────────────────
All game data lives under:
  games/
    thisorthat/rounds/{id}
    answerreveal/rounds/{id}
    guessmyanswer/rounds/{id}
    secretunlock/secrets/{id}
    dailyquestion/days/{YYYY-MM-DD}
    memoryquiz/sessions/{id}
    truthordare/rounds/{id}
    completesentence/rounds/{id}
    rapidfire/sessions/{id}
    randomquestion/asked/{id}
    moods/{YYYY-MM-DD-HH}
    ourworld/{roomId}

pandaMemory/{roomId}   ← Panda's shared memory doc
  { gameHistory, knownFacts, streaks, 
    totalPoints, matchRates, lastUpdated }

Add to firestore.rules:
  match /games/{gameType}/{collection}/{docId} {
    allow read, write: if isAllowed();
  }
  match /pandaMemory/{roomId} {
    allow read, write: if isAllowed();
  }

## ─────────────────────────────────────
## 💌 GAME MESSAGES IN CHAT
## ─────────────────────────────────────
Update lib/firestore.ts Message type, add:
  type: "game"  (alongside existing "text"|"image"|"voice")
  gameData: {
    gameType: string       // e.g. "thisorthat"
    gameName: string       // e.g. "This or That"
    emoji: string          // e.g. "🎲"
    result: string         // e.g. "Both chose: Late-night talk 🌙"
    pandaComment: string   // Panda's one-liner
    matched: boolean       // for confetti
  } | null

Update components/MessageBubble.tsx:
  If message.type === "game", render a game bubble:
  ┌─────────────────────────────┐
  │ 🎮 This or That             │
  │ Both chose: Late-night talk │
  │ "Great minds think alike!"  │  ← Panda comment
  └─────────────────────────────┘
  - Rounded card, border, subtle gradient bg
  - If matched === true: tiny confetti burst animation on mount
  - Not selectable for reactions (or optionally allow ❤️ only)

## ─────────────────────────────────────
## ✏️ CHANGES TO EXISTING FILES
## ─────────────────────────────────────
ONLY these minimal changes to existing files:

1. Chat.tsx:
   - Add gamePanelOpen: boolean state (default false)
   - In Sidebar component, add "🎮 Play a Game" button at bottom
   - On button click: setGamePanelOpen(true), setSidebarOpen(false)
   - In body layout: if gamePanelOpen, render 
     <GamePanel onClose={() => { 
       setGamePanelOpen(false); 
       setSidebarOpen(true); 
     }} />
     as the left panel BEFORE the chat column
   - Pass sendGameMessage callback to GamePanel:
     sendGameMessage(gameData) → calls sendMessage() 
     with type:"game" and gameData payload

2. lib/firestore.ts:
   - Add "game" to Message type union
   - Add gameData field to Message type
   - No other changes

3. components/MessageBubble.tsx:
   - Add game bubble rendering branch
   - No other changes

4. firestore.rules:
   - Add games/* and pandaMemory/* rules

## ─────────────────────────────────────
## 📦 NEW FILES TO CREATE
## ─────────────────────────────────────
src/lib/panda.ts
src/components/games/GamePanel.tsx
src/components/games/ThisOrThat.tsx
src/components/games/AnswerReveal.tsx
src/components/games/GuessMyAnswer.tsx
src/components/games/SecretUnlock.tsx
src/components/games/OneQuestionADay.tsx
src/components/games/MemoryQuiz.tsx
src/components/games/TruthOrDare.tsx
src/components/games/CompleteSentence.tsx
src/components/games/RapidFire.tsx
src/components/games/RandomQuestion.tsx
src/components/games/Scoreboard.tsx
src/components/games/MoodSync.tsx
src/components/games/BuildOurWorld.tsx
src/components/games/PandaAvatar.tsx (Panda loading/thinking animation)
src/components/games/GameBubble.tsx  (game result card in chat)

## ─────────────────────────────────────
## 🎨 DESIGN RULES FOR GAMES
## ─────────────────────────────────────
- Use the existing design system (CSS vars, Tailwind, shadcn/ui)
- Game panel background: slightly different from chat 
  (bg-card or bg-muted)
- Game cards: rounded-2xl, border border-border, shadow-sm
- Primary action buttons: use existing Button primary variant
- Panda AI responses: shown in a distinct bubble:
    bg-primary/10 border-l-4 border-primary rounded-r-2xl p-3
    with a small 🐼 icon on the left
- Loading state: Panda avatar with bouncing dots 
  ("Panda is thinking...")
- Animations: use Tailwind animate-* + custom keyframes:
    @keyframes floatUp, @keyframes flipReveal, 
    @keyframes confettiBurst, @keyframes lockOpen
- Keep all game UI mobile-friendly:
    tap targets minimum 44px
    text minimum 14px
    no hover-only interactions