I have an existing React + TypeScript + Tailwind + Firebase chat app 
called SweeTalk. The file structure is:

artifacts/sweetalk/src/
├── App.tsx                        (routing: /, /chat, /album)
├── pages/Chat.tsx                 (main chat UI)
├── pages/Login.tsx                (email/password login)
├── pages/Album.tsx                (shared photo grid)
├── components/MessageBubble.tsx   (message rendering + reactions)
├── components/EmojiPicker.tsx     (emoji popover)
├── components/VoiceRecorder.tsx   (hold-to-record audio)
├── components/SharedNote.tsx      (collaborative note)
├── components/TypingIndicator.tsx
├── lib/firebase.ts                (Firebase init)
├── lib/firestore.ts               (all Firestore helpers)
├── lib/storage.ts                 (uploadImage, uploadVoice)
├── hooks/useAuth.ts
├── hooks/useTheme.ts
├── hooks/useSound.ts
├── hooks/usePartnerUid.ts
├── contexts/AuthContext.tsx
firestore.rules
storage.rules
firebase.json

DO NOT touch what already works. Only add the features below.
Keep all existing code, logic, and structure intact.

## ─────────────────────────────────────
## 1. VIDEO & FILE SHARING
## ─────────────────────────────────────
In lib/storage.ts, add:
- uploadVideo(file: File): upload to Firebase Storage under videos/
- uploadDocument(file: File): upload to Firebase Storage under docs/

In lib/firestore.ts, update Message type:
- Add type: "video" | "document" to existing "text" | "image" | "voice"
- Add videoUrl: string | null
- Add documentUrl: string | null
- Add documentName: string | null
- Add documentSize: number | null

In sendMessage(), support new types.

In components/MessageBubble.tsx:
- Add video bubble: <video controls> with thumbnail preview, 
  max-h-64, rounded-xl
- Add document bubble: show file icon (based on extension), 
  filename, size (KB/MB), download button

In pages/Chat.tsx input bar:
- Add file picker button (📎) that opens a menu:
  • Video (accept="video/*", max 100MB)
  • Document (accept=".pdf,.doc,.docx,.xls,.xlsx,.zip")
- Show upload progress bar while uploading

## ─────────────────────────────────────
## 2. QUOTE / REPLY TO MESSAGES
## ─────────────────────────────────────
In lib/firestore.ts, add to Message type:
- replyTo: { id: string; text: string; senderId: string } | null

Update sendMessage() to accept optional replyTo payload.

In components/MessageBubble.tsx:
- Add swipe-right gesture (touchstart/touchmove/touchend, 
  threshold 60px) to trigger onReply callback
- Show quoted bubble above message:
  • Thin left border in primary color
  • Truncated text of the replied-to message
  • Tap the quote to scroll to original message (emit onJumpTo)

In pages/Chat.tsx:
- Add replyingTo state
- Show reply preview bar above input (with × to cancel)
- Pass replyTo to sendMessage()
- Handle onJumpTo to scroll to message by id

## ─────────────────────────────────────
## 3. STAR / BOOKMARK MESSAGES
## ─────────────────────────────────────
In lib/firestore.ts:
- Add starred: boolean to Message type
- Add starMessage(id: string, starred: boolean) function

In components/MessageBubble.tsx long-press menu:
- Add "⭐ Star" / "★ Unstar" action button
- Show ⭐ icon on starred messages

In App.tsx, add route /starred → ProtectedRoute

Create new page pages/Starred.tsx:
- List all messages where starred === true
- Same MessageBubble rendering
- Back button to /chat
- Empty state if no starred messages

In pages/Chat.tsx header, add ⭐ button → navigate to /starred

## ─────────────────────────────────────
## 4. MESSAGE SEARCH
## ─────────────────────────────────────
In pages/Chat.tsx header:
- Add 🔍 search icon button
- On click: show a search bar that slides down below the header
- As user types, filter current messages array in state 
  (client-side search on message.text)
- Highlight matching text in MessageBubble (wrap matches in 
  <mark> with bg-yellow-200/40)
- Show match count (e.g. "3 results")
- Up/down arrows to jump between matches (scrollIntoView)
- ESC or × to close search bar

## ─────────────────────────────────────
## 5. URL PREVIEW CARDS
## ─────────────────────────────────────
In lib/firestore.ts, add to Message type:
- linkPreview: { url: string; title: string; description: string;
  image: string | null } | null

In pages/Chat.tsx handleSend():
- Detect URLs in text using regex
- If URL found, call a Cloud Function or a public meta-scraper API
  (use allorigins.win proxy + DOMParser to extract og:title, 
  og:description, og:image from the URL's HTML)
- Store result in linkPreview field of the message

In components/MessageBubble.tsx:
- If message.linkPreview exists, render a card below the text:
  • Site favicon + domain name
  • og:image thumbnail (if exists)
  • Title (bold, 1 line, truncated)
  • Description (2 lines, truncated, text-muted-foreground)
  • Tap card → window.open(url)
  • Rounded-xl, border, subtle shadow

## ─────────────────────────────────────
## 6. TEXT FORMATTING
## ─────────────────────────────────────
Create lib/formatText.tsx:
- Export function formatText(text: string): React.ReactNode
- Parse and render:
  • *bold* → <strong>
  • _italic_ → <em>
  • ~strikethrough~ → <del>
  • `monospace` → <code className="bg-muted px-1 rounded text-xs font-mono">
- Return as array of React nodes (handle mixed text + formatted parts)

In components/MessageBubble.tsx:
- Replace plain <p>{message.text}</p> with 
  <p>{formatText(message.text)}</p>

In pages/Chat.tsx input:
- Add formatting toolbar (appears above input on focus):
  B / I / S / ` buttons that wrap selected text with markers
  or toggle if no selection

## ─────────────────────────────────────
## 7. EXPANDED REACTIONS + DOUBLE TAP
## ─────────────────────────────────────
In components/MessageBubble.tsx:
- Expand REACTION_EMOJIS to: ["❤️","😂","😮","😢","😡","👍"]
- Add a "+" button at end of quick reactions → open full EmojiPicker
  to pick any emoji as a reaction
- Add double-tap detection:
  • Track last tap time; if two taps within 300ms → trigger ❤️ reaction
  • Show floating ❤️ animation (position: absolute, animate up and fade)
  • Use CSS @keyframes floatUp: translateY(-40px) + opacity 0

## ─────────────────────────────────────
## 8. VOICE & VIDEO CALLS (WebRTC)
## ─────────────────────────────────────
Create lib/webrtc.ts:
- ICE servers config (Google STUN: stun:stun.l.google.com:19302)
- createOffer(roomId, localStream) → write offer SDP to 
  Firestore doc calls/{roomId}
- createAnswer(roomId, localStream) → write answer SDP
- subscribeToICECandidates(roomId, role, onCandidate)
- addICECandidate(roomId, role, candidate)
- endCall(roomId) → delete Firestore call doc

In lib/firestore.ts, add:
- Firestore collection: calls/{roomId} with 
  { offer, answer, callerCandidates[], calleeCandidates[], 
    status: "calling"|"active"|"ended", type: "voice"|"video",
    callerId, calleeId, startedAt, endedAt }
- subscribeToCallDoc(roomId, callback)
- Add calls subcollection to firestore.rules (both UIDs allowed)

Create components/CallManager.tsx:
- Mount this in Chat.tsx (always active when logged in)
- Listen to Firestore for incoming calls where calleeId === myUid 
  and status === "calling"
- Incoming call screen (full screen overlay):
  • Caller avatar + name
  • Pulsing ring animation
  • Accept (green phone) / Decline (red phone) buttons
- Active call UI:
  • Video call: local video (bottom-right corner, draggable PiP)
    + remote video (full screen background)
  • Voice call: avatar + name centered, waveform animation
  • Controls bar: Mute 🎤 | Speaker 🔊 | Flip camera 🔄 | End 📵
  • Live call duration timer (mm:ss)

In pages/Chat.tsx header:
- Add 📞 voice call button
- Add 📹 video call button
- On click: generate roomId, write call doc, start WebRTC offer

After call ends:
- Write "Missed call" or "Call ended (duration)" message 
  to Firestore messages collection

## ─────────────────────────────────────
## 9. PUSH NOTIFICATIONS (FCM)
## ─────────────────────────────────────
In lib/firebase.ts:
- Add: import { getMessaging, getToken, onMessage } from "firebase/messaging"
- Export: messaging = getMessaging(app)
- Export: requestNotificationPermission() → getToken(messaging, 
  { vapidKey: import.meta.env.VITE_FIREBASE_VAPID_KEY })
- Export: onForegroundMessage(callback) → onMessage(messaging, callback)

In lib/firestore.ts:
- Add: saveFCMToken(uid: string, token: string) 
  → setDoc users/{uid}/tokens/{token}

In contexts/AuthContext.tsx:
- After login: call requestNotificationPermission() + saveFCMToken()
- Call onForegroundMessage() → show in-app toast (do NOT show when
  app is in foreground and the message is from the current user)

Create public/firebase-messaging-sw.js service worker:
- Handle background push messages
- Show notification with: title = sender name, body = message text,
  icon = /favicon.svg, click → focus the app window

Add Cloud Function (functions/index.js):
- onNewMessage trigger (onCreate on messages collection):
  • Get partner's FCM tokens from Firestore
  • Send FCM notification via firebase-admin messaging.send()
  • Payload: title = sender displayName, body = message text
    (or "📷 Photo", "🎤 Voice message", "📎 File" based on type)

In lib/firestore.ts, add:
- muteNotifications(until: Date | null) → store in 
  userPreferences/{uid} doc
- getMuteStatus(uid) → read from userPreferences

In pages/Chat.tsx header or settings panel:
- Add 🔔 button → dropdown: 
  "Mute for 8 hours" | "Mute for 1 week" | "Unmute"
- Show 🔕 icon on header when muted

## ─────────────────────────────────────
## 10. FAVICON BADGE
## ─────────────────────────────────────
Create lib/faviconBadge.ts:
- drawBadge(count: number): draws on canvas over the favicon SVG
  • Red circle with white number on bottom-right of favicon
  • Use <canvas> to composite, then set as <link rel="icon">
- clearBadge(): restore original favicon

In pages/Chat.tsx useEffect for unreadCount:
- Call drawBadge(unreadCount) when count > 0
- Call clearBadge() when count === 0

## ─────────────────────────────────────
## 11. SECURITY: AES ENCRYPTION
## ─────────────────────────────────────
Create lib/crypto.ts:
- deriveKey(password: string, salt: Uint8Array): 
  Use PBKDF2 (Web Crypto API) → AES-GCM 256-bit CryptoKey
- encryptText(text: string, key: CryptoKey): 
  → base64 string (iv prepended to ciphertext)
- decryptText(ciphertext: string, key: CryptoKey): 
  → plaintext string
- Use a shared salt stored in Firestore (appConfig/encryption doc)
- Derive key from user's password at login time
- Store derived CryptoKey in React Context (never store raw key)

Update lib/firestore.ts sendMessage():
- Encrypt message.text before writing to Firestore

Update components/MessageBubble.tsx:
- Decrypt message.text using key from context before rendering

In contexts/AuthContext.tsx:
- After signIn(), derive CryptoKey and store in CryptoContext
- Export useCrypto() hook

## ─────────────────────────────────────
## 12. APP PIN LOCK
## ─────────────────────────────────────
Create components/PinLock.tsx:
- Full-screen overlay (z-50, bg-background)
- 4-dot PIN display + numpad (1-9, 0, backspace)
- On correct PIN (stored hashed in localStorage) → call onUnlock()
- On wrong PIN: shake animation + "Incorrect PIN" text
- After 3 wrong attempts: lock for 30 seconds
- "Use Biometric" button if WebAuthn available

Create hooks/usePinLock.ts:
- setupPin(pin: string): hash with SHA-256, store in localStorage
- verifyPin(pin: string): compare SHA-256 hash
- isLocked: boolean state
- lock(): set isLocked = true
- unlock(): set isLocked = false

In App.tsx:
- Wrap entire app with PinLock overlay if isLocked
- On app focus (visibilitychange → visible) after 5 min → lock()
- On first launch with no PIN set: show PIN setup screen

In Login.tsx:
- After successful Firebase auth, if no PIN set → redirect to /setup-pin
  (new page with PIN setup wizard: enter PIN, confirm PIN)

## ─────────────────────────────────────
## 13. BIOMETRIC LOCK (WebAuthn)
## ─────────────────────────────────────
In lib/biometric.ts:
- isWebAuthnAvailable(): check PublicKeyCredential support
- registerBiometric(): navigator.credentials.create() with 
  user = { id, name: "SweeTalk User" }
  Store credentialId in localStorage
- authenticateBiometric(): navigator.credentials.get()
  Return true if assertion passes

In components/PinLock.tsx:
- Show "Use Face ID / Fingerprint" button if isWebAuthnAvailable()
- On tap: call authenticateBiometric() → if success, onUnlock()

## ─────────────────────────────────────
## 14. CHAT THEMES & PERSONALIZATION
## ─────────────────────────────────────
Create lib/themes.ts:
Define 10 themes as CSS variable maps:
  default:  rose primary, cream background
  midnight: indigo primary, dark bg
  ocean:    cyan primary, deep blue bg
  forest:   emerald primary, dark green bg
  sunset:   orange primary, warm bg
  candy:    pink primary, light bg
  galaxy:   purple primary, near-black bg
  minimal:  gray primary, pure white bg
  gold:     amber primary, warm ivory bg
  rose:     deep rose primary, blush bg

Each theme defines:
  --primary, --primary-foreground, --background, 
  --card, --accent, --border, --muted

Create hooks/useTheme.ts (update existing):
- Add currentTheme state (persisted to localStorage)
- setTheme(name: string): apply CSS vars to document.documentElement
- Keep existing light/dark toggle working per-theme

Create components/ThemeSelector.tsx:
- Grid of 10 circular theme swatches
- Click to preview and apply
- Currently active theme has a ring

Create components/WallpaperSelector.tsx:
- 8 preset gradients (soft patterns as CSS gradients)
- Upload custom image option → store as dataURL in localStorage
- Apply as background to the messages scroll area
  (bg-cover bg-center, with slight opacity overlay for readability)

Create components/ChatSettings.tsx (slide-in panel or modal):
- Section: Theme (ThemeSelector)
- Section: Wallpaper (WallpaperSelector)  
- Section: Bubble color (color picker <input type="color"> for 
  sent bubble background, stored in localStorage)
- Section: Bubble shape (4 options: rounded/sharp/cloud/tail — 
  apply different border-radius classes)
- Section: Font size (small=13px / medium=15px / large=17px — 
  apply to .chat-text class)
- Section: Partner nickname (text input, stored in Firestore
  userPreferences/{uid})
- Section: Read receipts (toggle, stored in userPreferences)
- Section: Last seen (toggle, stored in userPreferences)

In pages/Chat.tsx:
- Add ⚙️ settings button in header → open ChatSettings panel
- Apply bubble color override via inline style or CSS var
- Apply bubble shape via conditional className
- Apply font size via CSS var

## ─────────────────────────────────────
## 15. PIN MESSAGES
## ─────────────────────────────────────
In lib/firestore.ts:
- Add Firestore collection: pinnedMessages (max 3 docs)
- pinMessage(messageId: string, messageText: string, senderId: string)
  → if count >= 3, reject with error "Max 3 pinned messages"
- unpinMessage(messageId: string)
- subscribeToPinnedMessages(callback)

In components/MessageBubble.tsx long-press menu:
- Add "📌 Pin" / "Unpin" action

In pages/Chat.tsx:
- Subscribe to pinnedMessages
- Show collapsible pinned banner below SharedNote:
  • If 1 pinned: show preview text + 📌 icon
  • If 2-3 pinned: show "📌 3 pinned messages" → tap to expand list
  • Each pinned item has "Jump to message" and "Unpin" actions
  • Slide-down animation on expand

## ─────────────────────────────────────
## 16. SHARED MEDIA & LINKS TABS
## ─────────────────────────────────────
Update pages/Album.tsx to be a tabbed page with 3 tabs:

Tab 1 - 📷 Photos (existing grid, keep as is)
Tab 2 - 🔗 Links:
  - Query messages where linkPreview != null or type contains URLs
  - Show list of link preview cards (title, domain, thumbnail)
  - Tap → open URL in new tab
  - Empty state if no links shared

Tab 3 - 📎 Files:
  - Query messages where type === "document"
  - Show list: file icon + name + size + download button
  - Group by date

Add bottom tab bar to the Album page with 3 tabs.
Keep the "Our Photo Album" button in the sidebar.

## ─────────────────────────────────────
## 17. VIRTUAL SCROLLING & PERFORMANCE
## ─────────────────────────────────────
Install react-window:
  pnpm add react-window @types/react-window

In pages/Chat.tsx:
- Wrap message list with react-window VariableSizeList
- estimateSize: ~60px base + extra for media, reactions, quotes
- After render, recalculate heights using a ResizeObserver on 
  each row's inner div
- Maintain scroll position when loading older messages

Add "Load more" trigger:
- When user scrolls to top of list → call loadMoreMessages() 
  from firestore.ts
- Prepend older messages to state without losing scroll position
- Show "Loading..." spinner at top while fetching

## ─────────────────────────────────────
## 18. PWA & OFFLINE MODE
## ─────────────────────────────────────
Create public/manifest.json:
{
  "name": "SweeTalk",
  "short_name": "SweeTalk",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#fff1f2",
  "theme_color": "#f43f5e",
  "icons": [
    { "src": "/favicon.svg", "sizes": "any", "type": "image/svg+xml" },
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ],
  "shortcuts": [
    { "name": "New Message", "url": "/chat", "icons": [{ "src": "/favicon.svg" }] },
    { "name": "Voice Call", "url": "/chat?call=voice" }
  ]
}

Update index.html:
- Add <link rel="manifest" href="/manifest.json">
- Add <meta name="theme-color" content="#f43f5e">

Create public/sw.js (Service Worker):
- Cache strategy: Cache First for assets (JS, CSS, fonts, images)
- Network First for API calls
- On install: cache all static assets
- On fetch: serve from cache, fallback to network
- Background sync: queue failed message sends in IndexedDB
  (sync tag: "send-message-sync")
  On sync event: retry all queued messages

In main.tsx:
- Register service worker:
  if ('serviceWorker' in navigator) navigator.serviceWorker.register('/sw.js')

In lib/offlineQueue.ts:
- Uses IndexedDB (idb library) to queue messages when offline
- queueMessage(payload): store in IDB
- processQueue(): iterate IDB, call sendMessage(), remove on success
- In pages/Chat.tsx handleSend(): if !navigator.onLine → queueMessage()
  show "Will send when online" toast

## ─────────────────────────────────────
## 19. UPDATE FIRESTORE RULES
## ─────────────────────────────────────
Update firestore.rules to add:
  // Pinned messages
  match /pinnedMessages/{docId} {
    allow read, write: if isAllowed();
  }
  // Starred messages (update existing messages rule to allow 
  //   starring by any authenticated user)
  // Calls
  match /calls/{callId} {
    allow read, write: if isAllowed();
  }
  match /calls/{callId}/callerCandidates/{candidateId} {
    allow read, write: if isAllowed();
  }
  match /calls/{callId}/calleeCandidates/{candidateId} {
    allow read, write: if isAllowed();
  }
  // User preferences (themes, nickname, mute settings)
  match /userPreferences/{uid} {
    allow read: if isAllowed();
    allow write: if isAllowed() && request.auth.uid == uid;
  }
  // FCM tokens
  match /users/{uid}/tokens/{tokenId} {
    allow read, write: if isAllowed() && request.auth.uid == uid;
  }
  // App config (shared encryption salt)
  match /appConfig/{docId} {
    allow read: if isAllowed();
    allow write: if false; // Only set manually
  }

## ─────────────────────────────────────
## 20. DELIVERABLES
## ─────────────────────────────────────
Produce updated versions of ONLY these files 
(do not rewrite unchanged files):

MODIFIED:
- src/App.tsx (new routes: /starred, /setup-pin)
- src/pages/Chat.tsx (all new features wired in)
- src/pages/Album.tsx (3 tabs: photos, links, files)
- src/pages/Login.tsx (PIN setup after first login)
- src/components/MessageBubble.tsx (video, doc, reply, 
  star, format, double-tap, expanded reactions)
- src/components/VoiceRecorder.tsx (waveform + speed toggle)
- src/lib/firestore.ts (updated Message type + new helpers)
- src/lib/storage.ts (video + document upload)
- src/contexts/AuthContext.tsx (crypto key + FCM init)
- firestore.rules (new collections)

NEW FILES:
- src/pages/Starred.tsx
- src/pages/SetupPin.tsx
- src/components/CallManager.tsx
- src/components/PinLock.tsx
- src/components/ChatSettings.tsx
- src/components/ThemeSelector.tsx
- src/components/WallpaperSelector.tsx
- src/lib/webrtc.ts
- src/lib/crypto.ts
- src/lib/biometric.ts
- src/lib/themes.ts
- src/lib/faviconBadge.ts
- src/lib/offlineQueue.ts
- src/lib/formatText.tsx
- src/hooks/usePinLock.ts
- public/manifest.json
- public/sw.js
- public/firebase-messaging-sw.js
- functions/index.js (FCM Cloud Function)